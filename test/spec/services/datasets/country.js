'use strict';

describe('Service: datasets/country', function () {

  // load the service's module
  beforeEach(module('beeOneWebFrontApp'));

  // instantiate service
  var datasets/country;
  beforeEach(inject(function (_datasets/country_) {
    datasets/country = _datasets/country_;
  }));

  it('should do something', function () {
    expect(!!datasets/country).toBe(true);
  });

});
