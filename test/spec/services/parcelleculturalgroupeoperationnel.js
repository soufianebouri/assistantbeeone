'use strict';

describe('Service: parcelleculturalgroupeoperationnel', function () {

  // load the service's module
  beforeEach(module('beeOneWebFrontApp'));

  // instantiate service
  var parcelleculturalgroupeoperationnel;
  beforeEach(inject(function (_parcelleculturalgroupeoperationnel_) {
    parcelleculturalgroupeoperationnel = _parcelleculturalgroupeoperationnel_;
  }));

  it('should do something', function () {
    expect(!!parcelleculturalgroupeoperationnel).toBe(true);
  });

});
